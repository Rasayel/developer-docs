import { readConfiguration } from '../../config/reader.js';
import path from 'path';
import { printError, printStacktrace } from './log.js';

async function loadFileConfiguration(configPath, showStacktrace) {
    try {
        return await readConfiguration(path.resolve(configPath));
    }
    catch (error) {
        if (error instanceof Error && !showStacktrace) {
            printError(error);
        }
        else {
            printStacktrace(error);
        }
        return null;
    }
}

export { loadFileConfiguration };
//# sourceMappingURL=loadConfigFile.js.map
