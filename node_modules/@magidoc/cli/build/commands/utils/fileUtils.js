import fs from 'fs';

function isDirectory(path) {
    return (fs
        .statSync(path, {
        throwIfNoEntry: false,
    })
        ?.isDirectory() ?? false);
}

export { isDirectory };
//# sourceMappingURL=fileUtils.js.map
