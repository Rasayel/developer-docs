import { existsSync } from 'fs';

function verifyDestinationAvailableTask(config) {
    return {
        title: `Verifying destination directory`,
        executor: () => {
            if (existsSync(config.destination)) {
                throw new Error(`Destination directory '${config.destination}' already exists.`);
            }
        },
    };
}

export { verifyDestinationAvailableTask };
//# sourceMappingURL=verifyDestinationAvailable.js.map
