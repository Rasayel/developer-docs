import type { Task } from '../runner';
import type { WebsiteConfiguration } from '../../config/types';
import type { ResolvedMagidocTemplateConfig } from './resolveTemplateConfig';
type Config = {
    website: Pick<WebsiteConfiguration, 'options'>;
};
type Ctx = {
    templateConfiguration: Pick<ResolvedMagidocTemplateConfig, 'envFileLocation' | 'supportedOptions'>;
};
export declare function writeEnvFileTask<T extends Ctx>(config: Config): Task<T>;
export {};
