import { toVariablesFile } from '@magidoc/plugin-starter-variables';
import { writeFile } from 'fs/promises';

function writeEnvFileTask(config) {
    return {
        title: `Write variables file`,
        executor: async (ctx) => {
            await writeFile(ctx.templateConfiguration.envFileLocation, toVariablesFile(config.website.options, ctx.templateConfiguration.supportedOptions));
        },
    };
}

export { writeEnvFileTask };
//# sourceMappingURL=writeEnvFile.js.map
