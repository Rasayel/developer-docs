import path from 'path';
import { moveOutputBuild } from '../../template/output.js';

function moveOutputTask(config) {
    return {
        title: `Move output`,
        executor: async (ctx, task) => {
            await moveOutputBuild(path.join(ctx.tmpDirectory.path, 'build'), config.website.output);
            task.output(`Moved output at ${config.website.output}`);
        },
    };
}

export { moveOutputTask };
//# sourceMappingURL=moveOutput.js.map
