export declare const PACKAGE_MANAGER_TYPES: readonly ["pnpm", "yarn", "npm"];
export type PackageManagerType = (typeof PACKAGE_MANAGER_TYPES)[number];
export type CommandConfiguration = {
    cwd: string;
    env?: Record<string, string>;
};
export type DevServerCommandConfiguration = CommandConfiguration & {
    host: string;
    port: number;
};
export type PackageManager = {
    type: PackageManagerType;
    runInstall: (config: CommandConfiguration) => Promise<void>;
    buildProject: (config: CommandConfiguration) => Promise<void>;
    startDevServer: (config: DevServerCommandConfiguration) => Promise<void>;
};
export declare function selectPackageManager(): Promise<PackageManager>;
export declare function getPackageManager(type: PackageManagerType): PackageManager;
export declare function isPackageManagerAvailable(type: PackageManagerType): Promise<boolean>;
