import extract from 'extract-zip';

async function unzipTemplate(config) {
    await extract(config.zipLocation, {
        dir: config.destination,
    });
}

export { unzipTemplate };
//# sourceMappingURL=unzip.js.map
