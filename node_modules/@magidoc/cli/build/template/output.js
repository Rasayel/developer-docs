import fs from 'fs-extra';

async function moveOutputBuild(from, to) {
    await fs.rm(to, { recursive: true, force: true });
    await fs.move(from, to);
}

export { moveOutputBuild };
//# sourceMappingURL=output.js.map
