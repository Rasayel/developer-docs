import { pathExists, copy } from 'fs-extra';
import { isDirectory } from '../commands/utils/fileUtils.js';

async function copyStaticAssets(from, to) {
    if (!(await pathExists(from))) {
        throw new Error(`Source path '${from}' does not exist.`);
    }
    if (!isDirectory(from)) {
        throw new Error(`Source path '${from}' is not a directory.`);
    }
    await copy(from, to, {
        overwrite: true,
    });
}

export { copyStaticAssets };
//# sourceMappingURL=assets.js.map
