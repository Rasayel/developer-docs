function enumConverter(values) {
    return {
        convert: (target) => {
            if (target === null || target === undefined) {
                return null;
            }
            if (!values.find((value) => value === target)) {
                return null;
            }
            return target;
        },
        asString: (value) => String(value),
        type: (z) => z.enum(values).optional(),
    };
}

export { enumConverter };
//# sourceMappingURL=enum.js.map
